# Contribution Log

> **Why this file exists:** the SE4050 assignment requires *individual
> contributions* (viva) and *weekly commits*. Every member appends one row per
> work session. This is the single place where who-did-what is recorded.

## How to use

1. Add one row at the **bottom** of the table each time you finish a work block.
2. Keep the row **one line** — no long paragraphs; a hyperlink to the relevant
   file/PR/notebook is fine.
3. **Never delete or edit other members' rows.** If a correction is needed, ask
   the member who wrote the row.
4. Weekly commit cadence: at least one commit per member per week with a row
   here to match.

---

| Date | Member | What was done | Files / links |
|---|---|---|---|
| 2026-09-10 | A | Scaffolded repo: structure, gitignore, configs, shared `src/`, READMEs, initial branch setup | `src/*`, `configs/*` |
| 2026-09-23 | C (Kalana) | Added Wish manifest audit, resumable train/validation-only runner, frozen four-model evaluator, prediction exports/plots, CLI demo, tests and report/notebook workflow. Implemented with Codex assistance; real-data training/evaluation remain pending. Dev merge was not approved. | `models/vit/`, `configs/wish/`, `tests/test_member_c.py`, `notebooks/04_vit_crossgen_evaluation.ipynb`, `docs/member_c_delivery.md` |
|  |  |  |  |
|  |  |  |  |

*(Add rows below. Keep sorted by date.)*
